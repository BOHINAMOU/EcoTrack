@model IEnumerable<EcoTrack.Models.Unite>
@{
    ViewData["Title"] = "Unités";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h3 class="mb-0">Unités</h3>
        <p class="text-muted mb-0">@Model.Count() unité(s) enregistrée(s)</p>
    </div>
    @if (User.IsInRole("AdminPrincipal"))
    {
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-lg"></i> Nouvelle unité
        </a>
    }
</div>

<div class="table-responsive-wrapper">
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Service</th>
                <th>Employés</th>
                <th>Statut</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var unite in Model)
            {
                <tr class="@(!unite.EstActif ? "table-secondary" : "")">
                    <td><a asp-action="Details" asp-route-id="@unite.Id">@unite.Nom</a></td>
                    <td>@unite.Service?.Nom</td>
                    <td>@unite.Employes.Count</td>
                    <td>
                        @if (unite.EstActif)
                        {
                            <span class="badge bg-success">Actif</span>
                        }
                        else
                        {
                            <span class="badge bg-secondary">Désactivé</span>
                        }
                    </td>
                    <td class="text-end">
                        @if (User.IsInRole("AdminPrincipal"))
                        {
                            <a asp-action="Edit" asp-route-id="@unite.Id" class="btn btn-sm btn-outline-primary">Modifier</a>
                            <form asp-action="BasculerActivation" asp-route-id="@unite.Id" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                @if (unite.EstActif)
                                {
                                    <button type="submit" class="btn btn-sm btn-outline-danger">Désactiver</button>
                                }
                                else
                                {
                                    <button type="submit" class="btn btn-sm btn-outline-success">Réactiver</button>
                                }
                            </form>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (!Model.Any())
{
    <p class="text-muted">Aucune unité enregistrée pour l'instant.</p>
}